/**
 * Badges + Shop (and coin grants) are paused until DB migrations are applied everywhere
 * and we bring the UX back. Set to `true` to re-enable sidebar, pages, coin increments, badge evaluation.
 */
export const FEATURE_SHOP_AND_BADGES = false;
